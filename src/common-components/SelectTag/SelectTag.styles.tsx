import styled from 'styled-components';
import { Tag as TagRaw, WhiteTag as WhiteTagRaw } from 'liber-components';

export const Tag = styled(TagRaw)`
  cursor: pointer;
`;
export const WhiteTag = styled(WhiteTagRaw)`
  cursor: pointer;
`;
